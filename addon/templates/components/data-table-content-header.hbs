<tr>
  {{#if enableSelection}}
    <th><!-- Checkbox --></th>
  {{/if}}
  {{#if enableLineNumbers}}
    <th><!-- Linenumbers --></th>
  {{/if}}
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{#each fields as |dtField|}}
      {{th-sortable field=dtField label=dtField currentSorting=sort}}
    {{/each}}
  {{/if}}
</tr>
